TxScript CreatePair(
  payer: Address,
  factory: TokenPairFactory,
  alphAmount: U256,
  tokenAId: ByteVec,
  tokenBId: ByteVec
) {
  factory.createPair{payer -> ALPH: alphAmount}(payer, alphAmount, tokenAId, tokenBId)
}